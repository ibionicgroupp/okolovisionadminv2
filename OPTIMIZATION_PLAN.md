# План оптимізації та покращень проекту

## 🔴 Критичні оптимізації (високий пріоритет)

### 1. **Дублювання коду - gameNames**
**Проблема:** Мапа `gameNames` дублюється в 3 місцях:
- `src/pages/user/[id].vue` (рядок 130)
- `src/pages/distributors/userStat.vue` (рядок 119)
- `cloud-functions/getStatistics/index.js` (рядок 6)

**Рішення:**
- Створити централізований файл `src/utils/gameNames.ts`
- Експортувати константу та використовувати в усіх місцях
- Для Cloud Functions - імпортувати або синхронізувати через shared модуль

**Очікуваний ефект:** Легше підтримувати, менше помилок при додаванні нових ігор

---

### 2. **Продуктивність Cloud Function - завантаження всіх користувачів**
**Проблема:** Функція `admingetstatisticsv2` завантажує всіх користувачів з Firestore (`collection('users').get()`), що при 1940 користувачах може бути повільно та дорого

**Рішення:**
- Додати індекси в Firestore для полів, що використовуються у фільтрах:
  - `subscription.isActive`
  - `gender`
  - `isClinic`
  - `birthday` (для віку)
- Розглянути можливість використання Firestore queries з фільтрами замість завантаження всіх документів
- Додати кешування результатів (Redis або Firestore cache) для частіших запитів
- Розбити на менші батчі при обробці великої кількості користувачів

**Очікуваний ефект:** Зменшення часу виконання з 6+ секунд до 1-2 секунд, зниження витрат

---

### 3. **Безпека - хардкоджені URL Cloud Functions** ✅ ВИКОНАНО
**Проблема:** URL Cloud Functions хардкоджені в коді:
- `src/pages/users.vue`: `adminlistuserslitev2-956914206562.europe-west1.run.app`
- `src/pages/statistics.vue`: `admingetstatisticsv2-956914206562.europe-west1.run.app`
- `src/pages/user/[id].vue`: `userGetData`, `userUpdateProfile`
- І багато інших...

**Рішення:**
- ✅ Створено централізований конфіг `src/utils/cloudFunctions.ts` з валідацією URL
- ✅ Оновлено всі файли для використання централізованого конфігу:
  - `src/pages/statistics.vue`
  - `src/pages/users.vue`
  - `src/pages/user/[id].vue`
  - `src/pages/promocodes.vue`
  - `src/pages/UserStats.vue`
  - `src/pages/distributors/index.vue`
  - `src/pages/distributors/[id].vue`
  - `src/pages/distributors/userStat.vue`
  - `src/services/DistributorsService.js`
- ✅ Створено `ENV_SETUP.md` з інструкціями по налаштуванню
- ⚠️ Потрібно створити `.env` файл з реальними значеннями (див. `ENV_SETUP.md`)

**Очікуваний ефект:** Легше змінювати URL, підтримка різних середовищ, краща безпека

---

### 4. **Дублювання логіки парсингу дат**
**Проблема:** Функції парсингу дат дублюються:
- `parseSubDate()` в `src/pages/user/[id].vue` (рядок 53)
- `parseSubDate()` в `src/pages/distributors/userStat.vue`
- `calculateAge()` в `cloud-functions/getStatistics/index.js` (рядок 29)
- `normalizeDate()` в різних місцях

**Рішення:**
- Створити утиліту `src/utils/dateUtils.ts` з усіма функціями парсингу
- Використовувати бібліотеку `dayjs` (вже в залежностях) для консистентності
- Експортувати функції для використання в Cloud Functions

**Очікуваний ефект:** Менше багів, легше підтримувати формати дат

---

## 🟡 Важливі оптимізації (середній пріоритет)

### 5. **Невикористані залежності та файли** ✅ ВИКОНАНО
**Проблема:**
- `src/composables/_useUsers.ts.bak` - backup файл
- Можливо невикористані бібліотеки в `package.json`

**Рішення:**
- ✅ Видалено backup файл `_useUsers.ts.bak`
- ⚠️ Для перевірки невикористаних залежностей потрібно запустити `npm-check-unused` вручну
- ⚠️ Очищення `package.json` - потрібен ручний аналіз

---

### 6. **Типізація TypeScript**
**Проблема:** Багато `any` типів:
- `src/pages/user/[id].vue`: `type UserData = any`
- `src/pages/users.vue`: `[k: string]: any`
- Відсутні інтерфейси для даних з Firebase

**Рішення:**
- Створити `src/types/` з інтерфейсами:
  - `User.ts` - інтерфейс користувача
  - `Subscription.ts` - інтерфейс підписки
  - `GameRecord.ts` - інтерфейс записів ігор
  - `Statistics.ts` - інтерфейс статистики
- Замінити `any` на конкретні типи

**Очікуваний ефект:** Краща підтримка IDE, менше помилок під час розробки

---

### 7. **Обробка помилок та логування**
**Проблема:** Непослідовна обробка помилок:
- Деякі місця використовують `console.error`
- Деякі показують тільки загальні повідомлення
- Відсутнє централізоване логування

**Рішення:**
- Створити сервіс логування `src/services/logger.ts`
- Додати Sentry або аналог для відстеження помилок у продакшені
- Стандартизувати формати помилок

---

### 8. **Оптимізація завантаження даних**
**Проблема:**
- `src/pages/users.vue` завантажує всіх користувачів одразу (може бути тисячі)
- Відсутня пагінація на бекенді
- Відсутнє кешування

**Рішення:**
- Додати віртуальну пагінацію або infinite scroll
- Реалізувати кешування в Pinia store
- Додати debounce для пошуку (вже є, але можна покращити)

---

### 9. **Дублювання axios конфігурації**
**Проблема:** Два різні підходи до HTTP запитів:
- `src/utils/api.js` - використовує `ofetch`
- `src/composables/useApi.js` - використовує `@vueuse/core`
- Більшість компонентів використовують `axios` напряму

**Рішення:**
- Стандартизувати на одному підході (рекомендую `ofetch` або `useApi`)
- Створити wrapper для Cloud Functions з обробкою помилок
- Додати retry логіку та timeout

---

### 10. **Оптимізація бандлу**
**Проблема:** Великий розмір бандлу через:
- Всі іконки завантажуються одразу
- Великі бібліотеки (Chart.js, ApexCharts, Vuetify)

**Рішення:**
- Code splitting для маршрутів
- Lazy loading компонентів
- Tree shaking для невикористаних частин бібліотек
- Оптимізація іконок (використовувати тільки потрібні)

---

## 🟢 Покращення (низький пріоритет)

### 11. **Тестування**
**Проблема:** Відсутні тести

**Рішення:**
- Додати Unit тести для утиліт (Vitest)
- Додати E2E тести для критичних флоу (Playwright)
- Тестування Cloud Functions локально

---

### 12. **Документація**
**Проблема:** Відсутня документація для:
- API endpoints
- Структури даних
- Процесів деплою

**Рішення:**
- Додати JSDoc коментарі до функцій
- Створити `docs/` з описом архітектури
- Документувати Cloud Functions

---

### 13. **Accessibility (A11y)**
**Проблема:** Можливо відсутні ARIA атрибути та підтримка клавіатури

**Рішення:**
- Додати ARIA labels
- Перевірити навігацію з клавіатури
- Додати focus management

---

### 14. **Міжнародізація (i18n)**
**Проблема:** Текст хардкоджений українською, хоча є `vue-i18n`

**Рішення:**
- Винести всі тексти в i18n файли
- Додати підтримку англійської мови
- Використовувати i18n для всіх повідомлень

---

### 15. **Моніторинг та аналітика**
**Проблема:** Відсутній моніторинг продуктивності

**Рішення:**
- Додати Google Analytics або аналог
- Моніторинг продуктивності Cloud Functions
- Алерти на критичні помилки

---

## 📊 Метрики для відстеження

### Продуктивність:
- [ ] Час завантаження сторінки < 2 сек
- [ ] Time to Interactive < 3 сек
- [ ] Cloud Function response time < 2 сек
- [ ] Розмір бандлу < 500 KB (gzipped)

### Якість коду:
- [ ] Покриття тестами > 70%
- [ ] ESLint помилок = 0
- [ ] TypeScript strict mode enabled
- [ ] Дублювання коду < 5%

### Безпека:
- [ ] Всі секрети в env змінних
- [ ] Валідація всіх вхідних даних
- [ ] Захист від XSS/CSRF
- [ ] Rate limiting на API

---

## 🎯 План впровадження (рекомендований порядок)

### Тиждень 1-2: Критичні оптимізації
1. Централізація `gameNames` (1 день)
2. Конфігурація Cloud Functions через env (1 день)
3. Оптимізація Cloud Function з індексами (2-3 дні)
4. Централізація утиліт дат (1 день)

### Тиждень 3-4: Важливі оптимізації
5. Типізація TypeScript (3-4 дні)
6. Стандартизація HTTP клієнта (2 дні)
7. Покращення обробки помилок (2 дні)
8. Оптимізація завантаження даних (2-3 дні)

### Тиждень 5+: Покращення
9. Тестування (постійно)
10. Документація (постійно)
11. Accessibility та i18n (за потреби)

---

## 💡 Додаткові рекомендації

1. **CI/CD Pipeline:**
   - Автоматичні тести перед деплоєм
   - Автоматичний деплой Cloud Functions
   - Перевірка типів TypeScript

2. **Code Review:**
   - Обов'язковий code review перед мерджем
   - Чеклист для перевірки

3. **Моніторинг:**
   - Налаштувати алерти на помилки
   - Відстежувати продуктивність
   - Аналізувати використання ресурсів

4. **Рефакторинг:**
   - Поступово рефакторити старі компоненти
   - Використовувати Composition API скрізь
   - Дотримуватися принципів SOLID
